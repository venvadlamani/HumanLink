'use strict';

/**
 * Base controller for the home module.
 */
angular
    .module('Home')
    .controller('faqCtrl', ['$scope', '$window', '$http',
        function ($scope, $window, $http) {

        }]);
