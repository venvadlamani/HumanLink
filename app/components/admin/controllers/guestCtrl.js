'use strict';

/**
 * Base controller for the home module.
 */
angular
    .module('Admin')
    .controller('guestCtrl', ['$scope', '$http', 'userSession',
        function ($scope, $http, userSession) {

        }]);
